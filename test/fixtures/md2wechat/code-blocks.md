代码块示例：

```javascript
function greet(name) {
  const msg = "Hello, " + name + "!";
  console.log(msg);
  return msg;
}
```

```python
def fibonacci(n):
    if n <= 1:
        return n
    return fibonacci(n - 1) + fibonacci(n - 2)
```
